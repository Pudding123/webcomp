/**
 * Created by yanghl on 16-5-25.
 */

define(function(require, exports, module){
    /**
     * 工具类
     * @class common.UIHelper
     */
    var UIHelper = {};

    /**
     * 弹出alert框
     * @method showAlert
     * @param  options {Object}
     * @static
     * @public
     * @example {
     *   type: UIHelper.TOASTER_SUC,
     *   title: "提示"，
     *   msg : "消息"
     *  }
     */
    UIHelper.showAlert = function (options) {
        options = options ||{};

    };

    return UIHelper;
});
